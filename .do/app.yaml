name: lw3-business-site
static_sites:
  - name: lw3-business-site
    build_command: pnpm run build
    output_dir: dist
    routes:
      - path: /
    catchall_document: index.html   # ← this is the magic line
